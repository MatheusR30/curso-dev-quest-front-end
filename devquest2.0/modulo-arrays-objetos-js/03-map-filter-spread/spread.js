// SPREAD operator - copiar elementos de um array ou objeto para outro array ou objeto
const numbers = [1, 2, 3];

// Estou pegando somente os elementos do array number e colocando dentro de um novo
// array newNumbers1
const newNumbers = [...numbers, 10, 20, 30];

console.log(newNumbers);
